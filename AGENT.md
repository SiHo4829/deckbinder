# AGENT.md — Multi-Agent Collaboration & Execution Guide

## 🤖 Sub-Agent Roles & Workflows

본 프로젝트는 아래 3가지 하위 에이전트 역할 분담을 통해 시스템 설계, TDD 개발, 품질 검증 파이프라인을 엄격히 준수합니다.

### 1. 🏗️ Architect (수석 소프트웨어 아키텍트)
* **역할:** 사용자의 요구사항을 분석하여 모듈 구조, DB 스키마, API 명세를 정의하고 `.claude/plan.md` 파일에 기록.
* **핵심 지침:**
  * 되팔이 방지(1회 1장 검색 제한, 8~12초 대기 연출) 및 단일 기준가 표기 정책이 시스템 설계에 반드시 반영되도록 정의.
  * 카드 DB의 키워드 필터 및 대체 카드 그룹화(`similar_group_id`) 스키마 설계.

### 2. 💻 Developer (풀스택 개발자)
* **역할:** `.claude/plan.md`에 정의된 구성을 바탕으로 실패하는 테스트 코드를 먼저 작성(TDD)한 뒤, 이를 통과하는 최적의 코드를 구현.
* **핵심 지침:**
  * `Next.js App Router`, `TypeScript`, `Supabase`, `Tailwind CSS` 컨벤션 준수.
  * 크롤러 모듈 구현 시 배치(Batch) 요청이나 일괄 스캔 로직을 절대 구현하지 않음.

### 3. 🔍 Reviewer (시니어 코드 리뷰어)
* **역할:** 작성된 코드가 프로젝트 컨벤션을 준수하는지, 보안 취약점(RLS, CORS 등)이 없는지 검토하고 테스트를 직접 실행하여 통과 여부 검증.
* **핵심 지침:**
  * TDD 테스트 코드 통과 여부 및 되팔이 차단 제약 조건 충족 여부 체크.
  * 주식형 시세 변동 차트나 외부 노출용 커뮤니티 게시판 같은 범위 밖(Out-of-scope) 코드 삽입 여부 검토.

---

## 🔄 Agent Execution Pipeline

| 단계 | 담당 | 산출물 | 완료 기준 |
|------|------|--------|-----------|
| 1. Planning | Architect | `.claude/plan.md` 갱신 | 구조 · 스키마 · API 계약이 태스크 단위로 분해됨 |
| 2. Implementation | Developer | 실패 테스트 → 통과 코드 | 해당 태스크의 테스트가 red → green |
| 3. Review | Reviewer | 리뷰 코멘트 | 컨벤션 · 보안 · 범위 위반 0건 |
| 4. Verification | Reviewer | 실행 로그 | `npm run lint` · `npm run test` · `npm run build` 전부 통과 |
| 5. Commit | 사람 | 커밋 | `git diff` 육안 확인 후 사람이 직접 커밋 |

### 파이프라인 운영 규칙

* **단일 기준 문서:** 구현은 `.claude/plan.md`를 따른다. 계획과 다른 구조가 필요해지면 코드보다 `plan.md`를 먼저 갱신하고 Architect 승인을 받는다.
* **테스트 필수:** 모든 신규 기능과 버그 수정에는 대응하는 테스트 코드가 존재해야 한다. 테스트 없는 구현은 Review 단계에서 반려한다.
* **브랜치 안전:** 메인 브랜치에 직접 푸시하지 않는다. 작업 브랜치에서 진행하고 사람이 병합한다.
* **시크릿 관리:** API 키 · 비밀번호 등 민감 정보는 `.env`(gitignore 대상)로 관리하며 코드에 하드코딩하지 않는다. 저장소에는 `.env.example`만 커밋한다.
* **범위 통제:** 범위 밖 기능(주식형 시세 변동 차트, 커뮤니티 게시판, 배치 크롤링, 벌크 조회 API)이 발견되면 병합 전에 제거한다.
* **명명 규칙:** 함수 · 변수 이름은 직관적으로 작성하며 불필요한 주석은 지양한다.

### 에이전트 호출 방법

서브에이전트 정의는 `.claude/agents/*.md`에 위치하며, 다음과 같이 위임한다.

* `architect 서브에이전트로 ○○ 설계해줘`
* `developer 서브에이전트로 T1.8 구현해줘`
* `reviewer 서브에이전트로 변경분 검토해줘`
